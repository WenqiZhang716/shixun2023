spring:
  application:
    name: AUTH-SERVICE

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql.sqlpub.com:3306/shixun2023?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: wqz716
    password: 611f47d0f0210920
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

server:
  port: 8001


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true

app:
  jwtSecret: demoSecretKey
  jwtExpirationMs: 86400000