spring:
  application:
    name: MANI-SERVICE

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql.sqlpub.com:3306/fatelearning?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: wenqi716
    password: a5823b277c540073
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8003


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true

app:
  jwtSecret: demoSecretKey
  jwtExpirationMs: 86400000